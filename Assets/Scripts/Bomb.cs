using UnityEngine;

public class Bomb : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("I am a very dangerous bomb!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
